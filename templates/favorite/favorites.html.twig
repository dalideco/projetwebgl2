<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/card.css">
   
    <style>
        *{
            box-sizing: border-box; 
            margin: 0 ; 
            padding: 0 ;
        }
        body{
            background: rgb(245, 245, 245);
            padding: 100px;
            background-image: url('images_AboutUs/cubes.png');
            font-family: sans-serif;
        }
        .card-flex{
            display:flex; 
            flex-wrap: wrap;
        }
        .card{
            
            animation: show 2s ease;
        }
        @keyframes show {
            0%{
                opacity: 0 ;
            }
        }
        
        .card a {
            background: rgb(61, 184, 108);
            border-radius: 15px; 
            padding: 5px 15px;
            color: white; 
            text-decoration: none;
            z-index:20;
            position: absolute;
            bottom: 10px; 
            left: 50%;
            transform: translateX(-50%) translateY(150%);
            opacity: 0 ;
            font-size: 25px;
            font-weight: 700;
            transition: background 300ms ease,opacity 1s ease,transform .75s ease;
        }
        .card a:hover{
            background:rgb(70, 209, 123)
        }
        .card:hover a {
            transform: translateX(-50%);
            opacity: 1 ;
        }
        body>h1{
            color:rgb(61, 184, 108);
        }
        @media(max-width: 700px){
            body{
                padding:50px;
            }
        }
        

        
    </style>
    <link rel="stylesheet" href="/css/gotohome.css">
</head>
<body>
    <h1>Favorites</h1>
    <div class="card-flex">
    {% for favorite in favorites %}
        <div class="card" style="background-image: url('/{{favorite.url}}')" data-aos="fade-up" >
            <div class="overlay">
                
            </div>
            <div class="content">
                <div class="lineDope"></div>
                <h1>{{favorite.title}}</h1>
            </div>
            <a href="/recipe/{{favorite.id}}">Visit</a>
        </div>
    {% endfor %}
    </div>
    <a href="/home" class="gotohome">x</a>

    
    
</body>
</html>